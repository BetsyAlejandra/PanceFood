import React from "react";
import { Link, Navigate, Route, Routes } from "react-router-dom";

export const MainApp = () => {
    return (
        <>
            <Routes>
                <Route path="/" />
            </Routes>
        </>
    )
}